version: '3.5'

networks:
   web-app-net:
       name: my-web-app-net
       #driver: overlay
       driver: bridge

volumes:
   mysql-db-storage:
   redis-inmemory-storage:

services:
   mysql-db:
      image: mysql:8.0.13
      environment:
          MYSQL_DATABASE: testDB
          MYSQL_ROOT_PASSWORD: root@123
      ports:
          - 3306:3306
      networks:
          - web-app-net
      volumes:
          - mysql-db-storage:/var/lib/mysql

   redis:
      image: redis:latest
      ports:
         - 6379:6379
      networks:
         - web-app-net
      volumes:
         - redis-inmemory-storage:/data

   zookeeper:
      image: bitnami/zookeeper:latest
      ports:
        - 2181:2181
      environment:
        - ALLOW_ANONYMOUS_LOGIN=yes
      networks:
        - web-app-net

   kafka:
      image: bitnami/kafka:latest
      ports:
        - 9092:9092
      environment:
        - KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181
        - ALLOW_PLAINTEXT_LISTENER=yes
        #Place Device-Current IP Or EC2-Private-IP
        #In case of device-ip, if we need to access the kafka from our host-machine for debug purpose.
        #In case of aws-ec2 instance, we have to broadcast on the ec2's private ip, other wise can't be listen from other ec2 instance.
        - KAFKA_ADVERTISED_LISTENERS= PLAINTEXT://192.168.1.18:9092
        #
      depends_on:
        - zookeeper
      networks:
        - web-app-net

   kafka-ui:
      image: provectuslabs/kafka-ui
      ports:
        - 8081:8080
      restart: on-failure
      environment:
        - KAFKA_CLUSTERS_0_NAME=local
        - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:9092
        - KAFKA_CLUSTERS_0_ZOOKEEPER=zookeeper:2181
        - KAFKA_CLUSTERS_0_READONLY=false
      depends_on:
        - kafka
      networks:
        - web-app-net

##End Of Scripts